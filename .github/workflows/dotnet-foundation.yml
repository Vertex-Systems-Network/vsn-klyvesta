name: dotnet-foundation

on:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - ".github/workflows/*.yml"
      - ".ai/state.json"
      - ".ai/checkpoints/**"
      - "global.json"
      - "Directory.Build.props"
      - "Directory.Packages.props"
      - "NuGet.config"
      - "src/**"
      - "tools/**"
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: dotnet-foundation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-verify:
    name: Build and architecture verify
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          global-json-file: global.json

      - name: Display SDK information
        run: dotnet --info

      - name: Restore API graph
        run: dotnet restore src/Klyvesta.Api/Klyvesta.Api.csproj

      - name: Restore architecture verifier
        run: dotnet restore tools/Klyvesta.ArchitectureVerifier/Klyvesta.ArchitectureVerifier.csproj

      - name: Restore paper broker verifier
        run: dotnet restore tools/Klyvesta.PaperBrokerVerifier/Klyvesta.PaperBrokerVerifier.csproj

      - name: Restore OMS verifier
        run: dotnet restore tools/Klyvesta.OmsVerifier/Klyvesta.OmsVerifier.csproj

      - name: Restore portfolio verifier
        run: dotnet restore tools/Klyvesta.PortfolioVerifier/Klyvesta.PortfolioVerifier.csproj

      - name: Restore risk verifier
        run: dotnet restore tools/Klyvesta.RiskVerifier/Klyvesta.RiskVerifier.csproj

      - name: Restore compliance verifier
        run: dotnet restore tools/Klyvesta.ComplianceVerifier/Klyvesta.ComplianceVerifier.csproj

      - name: Restore AI shadow verifier
        run: dotnet restore tools/Klyvesta.AiShadowVerifier/Klyvesta.AiShadowVerifier.csproj

      - name: Verify formatting
        run: |
          dotnet format src/Klyvesta.Api/Klyvesta.Api.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.ArchitectureVerifier/Klyvesta.ArchitectureVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.PaperBrokerVerifier/Klyvesta.PaperBrokerVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.OmsVerifier/Klyvesta.OmsVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.PortfolioVerifier/Klyvesta.PortfolioVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.RiskVerifier/Klyvesta.RiskVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.ComplianceVerifier/Klyvesta.ComplianceVerifier.csproj --verify-no-changes --no-restore
          dotnet format tools/Klyvesta.AiShadowVerifier/Klyvesta.AiShadowVerifier.csproj --verify-no-changes --no-restore

      - name: Build API graph
        run: dotnet build src/Klyvesta.Api/Klyvesta.Api.csproj --configuration Release --no-restore

      - name: Build architecture verifier
        run: dotnet build tools/Klyvesta.ArchitectureVerifier/Klyvesta.ArchitectureVerifier.csproj --configuration Release --no-restore

      - name: Build paper broker verifier
        run: dotnet build tools/Klyvesta.PaperBrokerVerifier/Klyvesta.PaperBrokerVerifier.csproj --configuration Release --no-restore

      - name: Build OMS verifier
        run: dotnet build tools/Klyvesta.OmsVerifier/Klyvesta.OmsVerifier.csproj --configuration Release --no-restore

      - name: Build portfolio verifier
        run: dotnet build tools/Klyvesta.PortfolioVerifier/Klyvesta.PortfolioVerifier.csproj --configuration Release --no-restore

      - name: Build risk verifier
        run: dotnet build tools/Klyvesta.RiskVerifier/Klyvesta.RiskVerifier.csproj --configuration Release --no-restore

      - name: Build compliance verifier
        run: dotnet build tools/Klyvesta.ComplianceVerifier/Klyvesta.ComplianceVerifier.csproj --configuration Release --no-restore

      - name: Build AI shadow verifier
        run: dotnet build tools/Klyvesta.AiShadowVerifier/Klyvesta.AiShadowVerifier.csproj --configuration Release --no-restore

      - name: Verify architecture boundaries
        run: dotnet run --project tools/Klyvesta.ArchitectureVerifier/Klyvesta.ArchitectureVerifier.csproj --configuration Release --no-build

      - name: Verify paper broker adapter
        run: dotnet run --project tools/Klyvesta.PaperBrokerVerifier/Klyvesta.PaperBrokerVerifier.csproj --configuration Release --no-build

      - name: Verify OMS state machine
        run: dotnet run --project tools/Klyvesta.OmsVerifier/Klyvesta.OmsVerifier.csproj --configuration Release --no-build

      - name: Verify portfolio projection and reconciliation
        run: dotnet run --project tools/Klyvesta.PortfolioVerifier/Klyvesta.PortfolioVerifier.csproj --configuration Release --no-build

      - name: Verify deterministic risk governor
        run: dotnet run --project tools/Klyvesta.RiskVerifier/Klyvesta.RiskVerifier.csproj --configuration Release --no-build

      - name: Verify deterministic compliance gate
        run: dotnet run --project tools/Klyvesta.ComplianceVerifier/Klyvesta.ComplianceVerifier.csproj --configuration Release --no-build

      - name: Verify AI shadow boundary
        run: dotnet run --project tools/Klyvesta.AiShadowVerifier/Klyvesta.AiShadowVerifier.csproj --configuration Release --no-build
